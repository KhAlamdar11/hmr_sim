from hmr_sim.utils.agent import Agent
import numpy as np

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

agent1 = Agent(agent_id=0, init_pos=np.array([0.0,0.0]), 
               speed=0,dt=0,vis_radius=1)

